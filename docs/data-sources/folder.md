---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "doit_folder Data Source - terraform-provider-doit"
subcategory: ""
description: |-
  Retrieve a Cloud Analytics folder by its ID.
---

# doit_folder (Data Source)

Retrieve a Cloud Analytics folder by its ID.

## Example Usage

```terraform
# Retrieve a folder by its ID
data "doit_folder" "example" {
  id = "your-folder-id"
}

# Output folder details
output "folder_name" {
  value = data.doit_folder.example.name
}

output "folder_parent" {
  value = data.doit_folder.example.parent_folder_id
}

# Use a folder ID when creating a report in that folder
resource "doit_report" "example" {
  name      = "Cost by Service"
  folder_id = data.doit_folder.example.id

  config {
    metric {
      type  = "basic"
      value = "cost"
    }
    time_range {
      mode           = "last"
      amount         = 1
      unit           = "month"
      include_current = true
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Folder ID.

### Optional

- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `description` (String) Folder description.
- `name` (String) Folder name.
- `parent_folder_id` (String) Identifier of the parent folder. Set to "root" if the folder is at the top level.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
