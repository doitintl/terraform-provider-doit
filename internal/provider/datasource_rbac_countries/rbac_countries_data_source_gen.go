// Code generated by terraform-plugin-framework-generator DO NOT EDIT.

package datasource_rbac_countries

import (
	"context"
	"fmt"
	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-framework/types/basetypes"
	"github.com/hashicorp/terraform-plugin-go/tftypes"
	"strings"

	"github.com/hashicorp/terraform-plugin-framework/datasource/schema"
)

func RbacCountriesDataSourceSchema(ctx context.Context) schema.Schema {
	return schema.Schema{
		Attributes: map[string]schema.Attribute{
			"countries": schema.ListNestedAttribute{
				NestedObject: schema.NestedAttributeObject{
					Attributes: map[string]schema.Attribute{
						"country_code": schema.StringAttribute{
							Computed:            true,
							Description:         "Canonical ISO 3166-1 alpha-2 country code.",
							MarkdownDescription: "Canonical ISO 3166-1 alpha-2 country code.",
						},
						"name": schema.StringAttribute{
							Computed:            true,
							Description:         "Canonical English display name.",
							MarkdownDescription: "Canonical English display name.",
						},
					},
					CustomType: CountriesType{
						ObjectType: types.ObjectType{
							AttrTypes: CountriesValue{}.AttributeTypes(ctx),
						},
					},
				},
				Computed: true,
			},
		},
		Description:         "Manage country-based access to tenants in your customer hierarchy.",
		MarkdownDescription: "Manage country-based access to tenants in your customer hierarchy.",
	}
}

type RbacCountriesModel struct {
	Countries types.List `tfsdk:"countries"`
}

var _ basetypes.ObjectTypable = CountriesType{}

type CountriesType struct {
	basetypes.ObjectType
}

func (t CountriesType) Equal(o attr.Type) bool {
	other, ok := o.(CountriesType)

	if !ok {
		return false
	}

	return t.ObjectType.Equal(other.ObjectType)
}

func (t CountriesType) String() string {
	return "CountriesType"
}

func (t CountriesType) ValueFromObject(ctx context.Context, in basetypes.ObjectValue) (basetypes.ObjectValuable, diag.Diagnostics) {
	var diags diag.Diagnostics

	if in.IsNull() {
		return NewCountriesValueNull(), diags
	}

	if in.IsUnknown() {
		return NewCountriesValueUnknown(), diags
	}

	attributes := in.Attributes()

	countryCodeAttribute, ok := attributes["country_code"]

	if !ok {
		diags.AddError(
			"Attribute Missing",
			`country_code is missing from object`)

		return nil, diags
	}

	countryCodeVal, ok := countryCodeAttribute.(basetypes.StringValue)

	if !ok {
		diags.AddError(
			"Attribute Wrong Type",
			fmt.Sprintf(`country_code expected to be basetypes.StringValue, was: %T`, countryCodeAttribute))
	}

	nameAttribute, ok := attributes["name"]

	if !ok {
		diags.AddError(
			"Attribute Missing",
			`name is missing from object`)

		return nil, diags
	}

	nameVal, ok := nameAttribute.(basetypes.StringValue)

	if !ok {
		diags.AddError(
			"Attribute Wrong Type",
			fmt.Sprintf(`name expected to be basetypes.StringValue, was: %T`, nameAttribute))
	}

	if diags.HasError() {
		return nil, diags
	}

	return CountriesValue{
		CountryCode: countryCodeVal,
		Name:        nameVal,
		state:       attr.ValueStateKnown,
	}, diags
}

func NewCountriesValueNull() CountriesValue {
	return CountriesValue{
		state: attr.ValueStateNull,
	}
}

func NewCountriesValueUnknown() CountriesValue {
	return CountriesValue{
		state: attr.ValueStateUnknown,
	}
}

func NewCountriesValue(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) (CountriesValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	// Reference: https://github.com/hashicorp/terraform-plugin-framework/issues/521
	ctx := context.Background()

	for name, attributeType := range attributeTypes {
		attribute, ok := attributes[name]

		if !ok {
			diags.AddError(
				"Missing CountriesValue Attribute Value",
				"While creating a CountriesValue value, a missing attribute value was detected. "+
					"A CountriesValue must contain values for all attributes, even if null or unknown. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("CountriesValue Attribute Name (%s) Expected Type: %s", name, attributeType.String()),
			)

			continue
		}

		if !attributeType.Equal(attribute.Type(ctx)) {
			diags.AddError(
				"Invalid CountriesValue Attribute Type",
				"While creating a CountriesValue value, an invalid attribute value was detected. "+
					"A CountriesValue must use a matching attribute type for the value. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("CountriesValue Attribute Name (%s) Expected Type: %s\n", name, attributeType.String())+
					fmt.Sprintf("CountriesValue Attribute Name (%s) Given Type: %s", name, attribute.Type(ctx)),
			)
		}
	}

	for name := range attributes {
		_, ok := attributeTypes[name]

		if !ok {
			diags.AddError(
				"Extra CountriesValue Attribute Value",
				"While creating a CountriesValue value, an extra attribute value was detected. "+
					"A CountriesValue must not contain values beyond the expected attribute types. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("Extra CountriesValue Attribute Name: %s", name),
			)
		}
	}

	if diags.HasError() {
		return NewCountriesValueUnknown(), diags
	}

	countryCodeAttribute, ok := attributes["country_code"]

	if !ok {
		diags.AddError(
			"Attribute Missing",
			`country_code is missing from object`)

		return NewCountriesValueUnknown(), diags
	}

	countryCodeVal, ok := countryCodeAttribute.(basetypes.StringValue)

	if !ok {
		diags.AddError(
			"Attribute Wrong Type",
			fmt.Sprintf(`country_code expected to be basetypes.StringValue, was: %T`, countryCodeAttribute))
	}

	nameAttribute, ok := attributes["name"]

	if !ok {
		diags.AddError(
			"Attribute Missing",
			`name is missing from object`)

		return NewCountriesValueUnknown(), diags
	}

	nameVal, ok := nameAttribute.(basetypes.StringValue)

	if !ok {
		diags.AddError(
			"Attribute Wrong Type",
			fmt.Sprintf(`name expected to be basetypes.StringValue, was: %T`, nameAttribute))
	}

	if diags.HasError() {
		return NewCountriesValueUnknown(), diags
	}

	return CountriesValue{
		CountryCode: countryCodeVal,
		Name:        nameVal,
		state:       attr.ValueStateKnown,
	}, diags
}

func NewCountriesValueMust(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) CountriesValue {
	object, diags := NewCountriesValue(attributeTypes, attributes)

	if diags.HasError() {
		// This could potentially be added to the diag package.
		diagsStrings := make([]string, 0, len(diags))

		for _, diagnostic := range diags {
			diagsStrings = append(diagsStrings, fmt.Sprintf(
				"%s | %s | %s",
				diagnostic.Severity(),
				diagnostic.Summary(),
				diagnostic.Detail()))
		}

		panic("NewCountriesValueMust received error(s): " + strings.Join(diagsStrings, "\n"))
	}

	return object
}

func (t CountriesType) ValueFromTerraform(ctx context.Context, in tftypes.Value) (attr.Value, error) {
	if in.Type() == nil {
		return NewCountriesValueNull(), nil
	}

	if !in.Type().Equal(t.TerraformType(ctx)) {
		return nil, fmt.Errorf("expected %s, got %s", t.TerraformType(ctx), in.Type())
	}

	if !in.IsKnown() {
		return NewCountriesValueUnknown(), nil
	}

	if in.IsNull() {
		return NewCountriesValueNull(), nil
	}

	attributes := map[string]attr.Value{}

	val := map[string]tftypes.Value{}

	err := in.As(&val)

	if err != nil {
		return nil, err
	}

	for k, v := range val {
		a, err := t.AttrTypes[k].ValueFromTerraform(ctx, v)

		if err != nil {
			return nil, err
		}

		attributes[k] = a
	}

	return NewCountriesValueMust(CountriesValue{}.AttributeTypes(ctx), attributes), nil
}

func (t CountriesType) ValueType(ctx context.Context) attr.Value {
	return CountriesValue{}
}

var _ basetypes.ObjectValuable = CountriesValue{}

type CountriesValue struct {
	CountryCode basetypes.StringValue `tfsdk:"country_code"`
	Name        basetypes.StringValue `tfsdk:"name"`
	state       attr.ValueState
}

func (v CountriesValue) ToTerraformValue(ctx context.Context) (tftypes.Value, error) {
	attrTypes := make(map[string]tftypes.Type, 2)

	var val tftypes.Value
	var err error

	attrTypes["country_code"] = basetypes.StringType{}.TerraformType(ctx)
	attrTypes["name"] = basetypes.StringType{}.TerraformType(ctx)

	objectType := tftypes.Object{AttributeTypes: attrTypes}

	switch v.state {
	case attr.ValueStateKnown:
		vals := make(map[string]tftypes.Value, 2)

		val, err = v.CountryCode.ToTerraformValue(ctx)

		if err != nil {
			return tftypes.NewValue(objectType, tftypes.UnknownValue), err
		}

		vals["country_code"] = val

		val, err = v.Name.ToTerraformValue(ctx)

		if err != nil {
			return tftypes.NewValue(objectType, tftypes.UnknownValue), err
		}

		vals["name"] = val

		if err := tftypes.ValidateValue(objectType, vals); err != nil {
			return tftypes.NewValue(objectType, tftypes.UnknownValue), err
		}

		return tftypes.NewValue(objectType, vals), nil
	case attr.ValueStateNull:
		return tftypes.NewValue(objectType, nil), nil
	case attr.ValueStateUnknown:
		return tftypes.NewValue(objectType, tftypes.UnknownValue), nil
	default:
		panic(fmt.Sprintf("unhandled Object state in ToTerraformValue: %s", v.state))
	}
}

func (v CountriesValue) IsNull() bool {
	return v.state == attr.ValueStateNull
}

func (v CountriesValue) IsUnknown() bool {
	return v.state == attr.ValueStateUnknown
}

func (v CountriesValue) String() string {
	return "CountriesValue"
}

func (v CountriesValue) ToObjectValue(ctx context.Context) (basetypes.ObjectValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	attributeTypes := map[string]attr.Type{
		"country_code": basetypes.StringType{},
		"name":         basetypes.StringType{},
	}

	if v.IsNull() {
		return types.ObjectNull(attributeTypes), diags
	}

	if v.IsUnknown() {
		return types.ObjectUnknown(attributeTypes), diags
	}

	objVal, diags := types.ObjectValue(
		attributeTypes,
		map[string]attr.Value{
			"country_code": v.CountryCode,
			"name":         v.Name,
		})

	return objVal, diags
}

func (v CountriesValue) Equal(o attr.Value) bool {
	other, ok := o.(CountriesValue)

	if !ok {
		return false
	}

	if v.state != other.state {
		return false
	}

	if v.state != attr.ValueStateKnown {
		return true
	}

	if !v.CountryCode.Equal(other.CountryCode) {
		return false
	}

	if !v.Name.Equal(other.Name) {
		return false
	}

	return true
}

func (v CountriesValue) Type(ctx context.Context) attr.Type {
	return CountriesType{
		basetypes.ObjectType{
			AttrTypes: v.AttributeTypes(ctx),
		},
	}
}

func (v CountriesValue) AttributeTypes(ctx context.Context) map[string]attr.Type {
	return map[string]attr.Type{
		"country_code": basetypes.StringType{},
		"name":         basetypes.StringType{},
	}
}
