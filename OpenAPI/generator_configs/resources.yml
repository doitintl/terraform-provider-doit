provider:
  name: doit
resources:
  budget:
    create:
      path: /analytics/v1/budgets
      method: POST
    read:
      path: /analytics/v1/budgets/{id}
      method: GET
    update:
      path: /analytics/v1/budgets/{id}
      method: PATCH
    delete:
      path: /analytics/v1/budgets/{id}
      method: DELETE
